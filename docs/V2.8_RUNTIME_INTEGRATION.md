# ASV V2.8 — Tecnomage Prime Web/runtime integration

Visual gate approval advances V2.7Y into the V2.8 runtime integration track.

## Runtime candidate

- primary: `public/assets/characters/tecnomage-prime-v2.8-runtime-q.glb`
- float-position compatibility fallback: `public/assets/characters/tecnomage-prime-v2.8-runtime-optimized.glb`
- primary size: 1,508,728 bytes (~1.439 MiB)
- SHA-256: `c521867422f071f69855087c0495a47f11cc907601951e10f525bef55999be7c`
- active geometry: 63,529 triangles
- skin: 71 joints
- embedded runtime animations: 8

## Controller changes

`src/tecnomage.ts` now prefers embedded animation clips through `THREE.AnimationMixer`, with short cross-fades and one-shot action clips. The legacy procedural rig remains as fallback.

The existing `src/main.ts` state machine already exposes the required eight runtime states, so it does not need to change for this integration.

## Local QA already completed

- GLB structural/accessor/reference audit: 0 errors
- quantized skin-weight sum error: max / mean / p99 = 0
- CPU skinning comparison vs float runtime: max sampled delta ~1.513e-05 model units
- V2.7Y dense deformation QA: 54 animation samples, target six clips PASS
- TypeScript parser pass: only expected module-resolution errors in the isolated build container because `three` is not installed there

## Publication state

Both V2.8 GLBs are now committed on `feat/asv-tecnomage-v2-8-runtime`:

- `tecnomage-prime-v2.8-runtime-q.glb` — 1,508,728 bytes
- `tecnomage-prime-v2.8-runtime-optimized.glb` — 2,068,312 bytes

The runtime load chain is therefore complete:

1. quantized V2.8 hero
2. float V2.8 compatibility hero
3. legacy Quaternius `tecnomage.gltf`
4. procedural debug fallback

## Remaining promotion gate

The repository's Vercel status currently reports failure because the connected Vercel account hit a **build-rate-limit / plan quota gate**, not because a source/build error was returned.

Keep PR #7 in draft until a fresh repository build can run and the browser smoke test verifies all eight gameplay states against the deployed preview. Do not merge on the quota failure alone.
